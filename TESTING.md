Testing

Backend
- Run unit tests:
  - `mvn -q test` (requires Maven on PATH)

Frontend
- Build check:
  - `npm run build` from `frontend`

Notes
- This repo currently has limited automated tests; add more coverage for campaign flows and notifications.
